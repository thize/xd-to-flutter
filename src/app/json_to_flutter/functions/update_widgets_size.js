//TODO: update_widgets_size

function update_widgets_size(no, ant) {    
    /*no.children.forEach(function (prox) {
        update_widgets_size(prox, no);
    });*/
}

module.exports = { update_widgets_size };